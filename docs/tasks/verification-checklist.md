# CoffeeVision 検証（目視 QA）項目一覧

**実機 / シミュレータでの目視・手動検証はすべてこのファイルに集約する**（2026-07-08 分離 / 2026-07-25 に完全集約 / 2026-08-06 に実行順へ再編）。

- **`tasks.md` に目視行を置かない**。実装が終わって残るのが目視だけになったら、その時点でこのファイルへ 1 項目として移す（`tasks.md` 側はサマリで「残る目視は verification-checklist」と触れるだけ）
- ここに載る項目はいずれも **ビルド・自動テストは green 済み**。実装の進捗は [`tasks.md`](../tasks.md)、設計判断は [`implementation_note.md`](../implementation_note.md) が正
- **完了した項目は削除する**（`[x]` で残さない）。確認済みという事実だけが残っても次の QA で読み飛ばす行が増えるだけで、確認内容の記録が要るものは implementation_note に書く
- **コード内の行番号を書かない**（リファクタで即陳腐化する。2026-07-25 に `MapTabView.swift:1383` が実際に壊れた）。ファイル名か型名で指す

---

## 実行順の設計（2026-08-06 再編）

**トピック別ではなく前提条件別に並べる**。理由は、目視項目の多くが「一度きりしか観察できない状態」に依存しているため。

- **初回起動フローは 1 インストールにつき 1 回**しか見られない（同意オンボーディング / 広告プレプロンプト / ATT）
- **許可ダイアログは許可を出した瞬間に消える**。位置情報を許可する前・写真を許可する前にしか確認できない項目がある
- **記録 0 件の空状態**は記録を 1 件作った時点で失われる

トピック順に上から消化すると、これらを見落として**再インストールをやり直す**ことになる。以下の順なら**再インストールは 2 回**（パス 1 とパス 7）で全項目を消化できる。

| パス | 前提 | 内容 |
|------|------|------|
| 1 | **削除 → 再インストール**（1 回目） | 初回起動フローと空状態。**許可を出す前**の挙動を先に取る |
| 2 | なし | 記録の作成・編集・複製・削除。項目数が最も多い |
| 3 | Location 設定 + 実 Places キー | マップ / カフェ探索 |
| 4 | 記録が数件たまった状態 | 分析タブ |
| 5 | ダミーデータ Scheme | 30 件が要る項目 |
| 6 | **実機**（シミュレータ不可） | Apple Intelligence / Sign in with Apple / 長時間利用 |
| 7 | **削除 → 再インストール**（2 回目） | レビュー依頼の 1 回きり挙動とキルスイッチ |
| 8 | 後日 | Firebase コンソールの観察 |
| 9 | 通し | アクセシビリティ |

**パス 1 の中も順序が意味を持つ**。①同意オンボーディング → ②位置情報を許可する前にエディタを開く → ③記録 0 件の空状態 → ④初回の写真追加（許可ダイアログ）→ ⑤ここで各種許可を出してパス 2 へ。

**共通の前提**:

- 位置情報に依存する項目はシミュレータの **Features > Location（Custom Location）設定**が前提（未設定だとサジェスト等が出ない — lessons 2026-07-06）
- Apple Intelligence / Sign in with Apple 依存はシミュレータ不可 → **実機必須**
- 広告・Remote Config 依存はテスト ID / デモユニットでの確認になる（`No fill` はコード無罪の可能性 — implementation_note 2026-07-22）

---

## パス 1: クリーンインストール直後 ★再インストール 1 回目

> **この順に上から実行する**。位置情報・写真の許可を出すのはこのパスの最後。

- [ ] **初回起動フロー**（同意オンボーディング）: データ利用同意シートが表示される / **プライバシーポリシー導線**（2026-08-06 公開）— 「プライバシーポリシーを読む」をタップして**実ページが開く**こと（従来は `https://example.com/privacy` で 404 だった）
- [ ] **ブランド色の統一 ②**（MP-5 / 2026-07-27 修正）: 同意オンボーディングと広告プレプロンプトの**アイコン・CTA ボタンがアクセント色**になっている。**ダークモードで確認すること**（`.brown` 固定色と AccentColor のダーク値 #C08552 のずれを解消した修正のため、実害が出るのはダーク側）
- [ ] **15-B ①（位置情報を許可する前）**: **位置情報未許可でエディタを開いてもチップ・ダイアログが出ない**こと。※② 許可済みでチップ表示は 2026-07-06 確認済み
- [ ] **記録 0 件の空状態**: **15-C** 記録 0 件の表示 / **15-D（空状態プログレス）** 記録 0/1/2 件でプログレス表示 → 3 件到達でカテゴリ track の変化 / 信号が出たらバナー消失 / **2-13** タグ 0 件でサジェストのチップ行が出ないこと
- [ ] **ASO-6 ④（初回の写真追加時）**: 写真ライブラリの**許可ダイアログ**が「カフェで撮った写真を**コーヒー記録**に添えるため…」になっていること（旧「訪問記録」からの是正）
- [ ] **ASO-1 ③**: 記録が少なく傾向信号が出ていない状態で、分析タブを開いてもレビューダイアログが**出ない**こと

## パス 2: 記録の作成・編集・複製（前提なし）

- [ ] **フェーズ7（CoffeeRecord 主体）**: FAB→セルフ抽出保存→一覧 / カフェ詳細→記録 / マップピン / 詳細編集・削除 / Firestore `coffees`
- [ ] **フェーズ9（テイスティング 5 要素）**: 入力 / 追加・削除切替 / 詳細 / 分析グラフ / round-trip / VoiceOver
- [ ] **15-B（記録摩擦低減）**: ③ FAB→サジェストタップ→星＋写真だけで保存の最短パス ④ 詳細「…」メニュー→「これをもとに記録」で複製初期値（引き継ぎ 9 項目 / rating・notes・photos・tasting 空 / visitedOn = 今日）⑤ 新規作成の name 初期値「本日のコーヒー」
- [ ] **フェーズ6 既知バグ（buildCafe）**: セルフ抽出記録を編集→手動カフェ名入力→保存でカフェが残る / 複製でも同様 / 既存の Places 選択・引き継ぎ元ありの正常系が回帰しない（2026-07-08 修正）
- [ ] **2-13 過去に使ったタグのサジェスト（2026-07-26）**: タグ 3 種類以上を含む記録を数件作る → 新規エディタのタグ入力欄の下にチップが**使用回数の多い順**で出る / チップタップで付与されサジェストから消える・入力欄がクリアされる / 付与済みを外すとサジェストに戻る / 入力欄に文字を打つと部分一致で絞られる / **タグを 11 種類以上使った状態で、11 位以下のタグも入力すれば出せる**（絞り込み → 上限適用の順序の確認）/ 編集モードで開いたとき既に付いているタグがサジェストに出ない / ダーク・VoiceOver
- [ ] **写真の保存時リサイズ + 枚数上限（2026-08-01）**: ①**縦向きで撮った写真**を添付し、サムネ・詳細・全画面・共有カードのすべてで**横倒しにならない**こと（`kCGImageSourceCreateThumbnailWithTransform` の効き。**今回最もリグレッションしやすい箇所**）②写真を 10 枚添付すると「写真を追加」が無効化され、ラベルで上限が伝わること。11 枚目を選ぼうとしても増えないこと ③既存の記録の写真が従来どおり表示されること（保存済みファイルは無変更）④添付した写真を全画面で拡大して画質が許容できること ⑤**実測**: 1 枚添付後にコンテナの `<Documents>/photos/*.jpg` を `sips -g pixelWidth -g pixelHeight <path>` と `ls -lh` で確認し、**長辺 2048px 以下 / 想定 0.4〜1.3MB**（サンドボックスから PhotosPicker を操作できず UI 経由の実測が未取得のため。ImageIO アルゴリズム自体の検証は実施済み = implementation_note 2026-08-01）
- [ ] **星評価のレイアウト固定（2026-07-26 修正）**: エディタで未評価 → 星タップ → クリアボタンで未評価に戻す、の往復で**星の位置が動かない**こと / 未評価時にクリアボタン位置をタップしても反応しないこと / VoiceOver で未評価時に「評価を未評価に戻す」が読み上げられないこと（lessons 2026-07-26）
- [ ] **ASO-6 ①②③⑤⑥ 期待値管理のコピー（2026-08-01）**: ①設定タブに「データの保存先」セクションが出て、2 行（コーヒー記録=クラウドに保存 / 写真=この端末のみ）と説明文が読めること ②エクスポートの説明文が「写真は端末のバックアップで残してください」に変わっていること ③**匿名アカウントの状態で** 設定→アカウント管理 を開き、「写真はこの端末に残るため、他のデバイスには表示されません」まで表示されること ⑤ライト / ダーク / **Dynamic Type 最大**での折り返し ⑥VoiceOver で各行が 1 要素として読まれること
- [ ] **ASO-1 ⑥**: 写真ピッカーで複数枚選んだ直後にエディタを閉じても、取り込み処理が残らないこと（`.task(id:)` 化の回帰確認。写真がディスクに書かれるのは保存ボタン経路のみなのでファイルは元々漏れない）
- [ ] **15-C（検索＋月別）**: 検索ヒット / 検索ヒット 0 件 / 月跨ぎのセクション表示 / 検索中の FAB 挙動 / VoiceOver でのヘッダ読み上げ（Location 非依存。記録 0 件の状態はパス 1 で確認済み）
- [ ] **記録の同期往復**: 記録作成 → マップに訪問済みピンが立つ / Firestore コンソールで記録削除 → ローカル一覧から消える（shared レビュー対応の確認）
- [ ] **iosApp レビュー対応**: CafeDetail push→pop 後の一覧更新 / サインアウト・削除時のオーバーレイと完了処理 / スワイプ削除失敗時の写真残存（初回同意シート表示はパス 1 で確認済み）
- [ ] **15-E-2（エクスポート）**: エクスポート → JSON 内容が記録と一致（**`region` を含むこと** = 2026-07-25 修正の確認）/ 0 件時の挙動

## パス 3: マップ / カフェ探索（Location 設定 + 実 Places キー）

- [ ] **マップ検索結果の下部ドラッグシート（2026-07-22 刷新）**: テキスト検索でカメラが全結果にフィット / シートのドラッグ（peek ⇄ expanded）とハンドルタップでの detent トグル / 行タップ ⇄ ピンタップ ⇄ 選択カードの排他表示 / ✨ `TasteSearchSheet` と競合しない / **結果 3 件以上でスクロール到達時に広告が表示される**（fold 下の遅延ロード対応の回帰確認 — lessons 2026-07-22）/ ダーク・VoiceOver
- [ ] **フェーズ14（「このエリアを検索」）**: エリア検索→複数ピン / パン後のボタン再出現 / キーワードを維持して再検索される（2026-07-24 変更）/ 表示範囲外の結果が混ざらない（実 Places API キー必要）
- [ ] **17-D（POI タップ解決）**: 3 ピン（夢やカフェ / ふわランドリー&カフェ / ごはんカフェ くるま）がそれぞれ別の正しい店を開く。波及メモ: 型フィルタ解除で bakery も解決可能になるため、`AppleNearbyCafeLoader` の `.cafe` 限定は再検討候補（据え置き）
- [ ] **おすすめカフェ 9 県の投入結果（2026-08-01 投入 / 421 件）**: 東京 / 大阪 / 京都 / 神奈川 / 愛知 / 福岡 / 北海道 / 千葉 / 埼玉 の各県でおすすめピンが出ること（**ズームゲート 3000m のため広域表示では出ない**）/ **大手チェーンのピンが無いこと**（コメダ・ドトール・ベローチェ・星乃・エクセルシオール・タリーズ・上島・ルノアール・倉式。`STARBUCKS RESERVE ROASTERY TOKYO` = 中目黒のみ意図的に残置）/ 未投入県（例: 宮城・広島）でおすすめピンが 0 件であること。**メモリキャッシュ（one-shot get）のため投入後はアプリ再起動が必要**
- [ ] **マップピンの主従関係（MP-1〜3 / 2026-07-27 修正）**: 訪問済み・好み一致・保存済み・おすすめ・検索結果・周辺の 6 種を同一画面に出し、**訪問済み（36pt）がおすすめ（34pt）より大きく見える**こと / 全ピンに白フチが付き、有無が混ざらないこと / おすすめピンの影が他の意味ピンと同じ強さに見えること（`ui-ux-guidelines.md`「ピンの意匠ルール」の序列どおりか）/ ライト・ダーク両方
- [ ] **ブランド色の統一 ①**（MP-4 / 2026-07-27 修正）: 「行きたい」リストシートの「記録あり」バッジが**訪問済みピンと同じアクセント色**で表示される。**ダークモードで確認すること**

## パス 4: 分析タブ（記録が数件たまった状態）

- [ ] **フェーズ8（A-3/A-4/B-2/B-4）**: タブ / グラフ / 空状態 / 一致ピン / 理由シート / VoiceOver（Apple Intelligence の要約・Q&A はパス 6）
- [ ] **15-E-3（探索提案・BeanProfile 投入後）**: 好み信号あり＋未記録 BeanProfile で提案表示 / 記録済みは除外 / 0 件時（beanProfiles 未投入 / 好み未確定）はセクション非表示。あわせて分析タブ「好みの豆の傾向」も確認（**本番 Firestore への seed 投入が前提** — 2026-07-30 に 38 件投入済み。投入後はメモリキャッシュのためアプリ再起動が必要）。**確認は seed がカバーする 21 産地の記録で行う**（ベトナム / メキシコ 等の未カバー産地が bestOrigin になると、投入済みでも両セクションが空になり誤判定する）。※エディタの産地サジェストは 2026-07-22 の産地ドロップダウン化で撤去済みのため対象外

## パス 5: ダミーデータ Scheme

- [ ] **ダミーデータ Scheme**: 「iosApp (Dummy Data)」で 30 件・通常 Scheme で 0 件
- [ ] **ASO-1 ①⑤ レビュー依頼（9-8 / 2026-08-01）**: ①ダミーデータ Scheme で分析タブを開き、傾向信号が出ている状態で **1.5 秒後に**レビューダイアログが出ること（シミュレータでも表示される。実際の送信はされない）⑤ダイアログ表示までの 1.5 秒以内に**別タブへ移動**したとき、他タブの上にダイアログが出ないこと（2026-08-01 の B-8 で `.task(id:)` に統合し、**全経路でキャンセルされる**ようになった。以前あった「`.onChange` 経由では出うる」制約は解消済み）

## パス 6: 実機必須（シミュレータ不可）

- [ ] **フェーズ8 の AI 部分**: Apple Intelligence 有効実機での要約・Q&A round-trip
- [ ] **アカウント削除の revoke 完走 + 削除範囲**（フェーズ 5.2 / ガイドライン 5.1.1(v)）: **失敗すると審査でガイドライン違反になる最重要項目**。
  - **前提**: 匿名で起動 → **記録を数件 + 行きたい店を数件**作る（両方ないと削除範囲を検証できない）→ Sign in with Apple でアップグレード
  - **正常系**: 設定→アカウント削除→確認 → **Apple のサインインシートが再度出る**（出なければ匿名経路に落ちている = 即 NG）→ 処理中オーバーレイのまま**エラーアラートが一度も出ず**完了 → 新しい匿名 uid の初期状態に戻る
  - **合否は外部で確認する**: ①Firestore で **`users/{uid}` が丸ごと消えている**こと（ルート doc / `coffees` / `savedCafes` の 3 つとも。2026-08-06 是正 — 以前は後ろ 2 つが残っていた）②Firebase Console → Authentication のユーザー一覧から uid が消えている ③**iOS 設定 App → Apple アカウント → サインインとセキュリティ → 「Apple でサインイン」の一覧から CoffeeVision が消えている**
  - **③ が revoke が効いた唯一の直接証拠**。①②が通っていても③にアプリが残っていれば `revokeToken` が実質失敗している（Firebase Console の OAuth コードフロー設定を疑う）。消えていれば、次に Apple サインインしたとき「名前とメールを共有」の初回フローが再度出るのでそれも傍証になる
  - **端末ローカル**: 削除後にマップを開き、**行きたい店ピンが残っていない**こと（ローカル DB の孤児行が無いことの確認）
  - **異常系**: Apple シートをキャンセル → **アラートを出さず**オーバーレイが消え、記録・行きたい店・アカウントがすべて残っていること（revoke 失敗時の削除中断は、Firebase Console の設定を一時的に壊す必要があるため任意。やる場合は最後に必ず正常系を通し直す）
- [ ] **周辺カフェピンのスロットリング耐性**: 実機で長時間パン・ズームしてもピンが消えない（`MKLocalSearch` のスロットリングは意図的再現が困難なため、通常利用の中で観察する）

## パス 7: レビュー依頼の 1 回きり挙動 ★再インストール 2 回目

> **キルスイッチ（④）が最重要**。出荷後にレビュー依頼を止める唯一の手段なので、ここが効かないと制御不能になる。

- [ ] **ASO-1 ②④**: ②アプリ削除 → 再インストール後、一度ダイアログが出たら**2 回目以降は出ない**こと ④Firebase コンソールで `review_prompt_enabled = false` を配信 → **出ないこと**。**fetch 間隔は SDK 既定 12h** なので、確認時はアプリ削除 → 再インストールするか `minimumFetchInterval` を一時的に 0 にする

## パス 8: 後日 / コンソール観察

- [ ] **フェーズ18 の残務（コンソール観察）**: Crashlytics のクラッシュ到達（アプリ内導線が無いため実際のクラッシュ待ち）/ Firebase Performance のトレース反映（反映遅延があるため後日コンソールで確認）　※Analytics の consent ON→OFF gating と `screen_view` 発火は 2026-07-21 確認済み

## パス 9: 通し検証

- [ ] **アクセシビリティ通し検証**: VoiceOver / Dynamic Type / Reduce Motion

---

## 実行順に組み込めないもの

上のパスに入らない項目。前提が特殊なため個別に段取りが要る。

- [ ] **15-E-1（抽出レシピ `brewRecipe`）の migration 確認**: 入力→保存→round-trip / 複製で引き継ぎ までは**パス 2 で消化できる**。残る「既存記録（brewRecipe 無し）が migration 4.sqm 適用後も開ける」は**旧スキーマの DB が要る**ため、クリーンインストール後の端末では再現できない（旧ビルドを入れて記録を作ってから新ビルドへ差し替える段取りが必要）。なお migration の正しさは自動テスト（`verifySqlDelightMigration` + migration5 テスト）で JVM / NativeSqliteDriver の両方について検証済みで、**未リリースのため旧スキーマの実ユーザーは存在しない**
- [ ] **Android × Firestore 読み取り**: `data-firebase` の `observe` 経由の Firestore 読み取りがエミュレータ / 実機で動作 + Firestore Console で読み取り目視。**Android はリリース対象外**（KMP 共通レイヤーの検証ターゲット）なので、iOS リリースのブロッカーではない
